{
  "name": "emojibot-panel",
  "version": "2.0.0",
  "description": "Admin panel for EmojiBot2",
  "scripts": {
    "build": "tsc && cp src/index.html dist/ && cp src/app.js dist/",
    "serve": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "@types/ws": "^8.18.1",
    "typescript": "^5.2.2"
  }
}
